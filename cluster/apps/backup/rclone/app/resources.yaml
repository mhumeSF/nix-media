---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: gato-bucket-pv
spec:
  capacity:
    storage: 100Gi
  accessModes:
  - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  hostPath:
    path: /gato-bucket
    type: Directory
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - k3s
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gato-bucket-pvc
  namespace: backup
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: ""
  volumeName: gato-bucket-pv
  resources:
    requests:
      storage: 100Gi
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: rclone-config
  namespace: backup
data:
  rclone.conf: |
    [b2]
    type = b2
    account = ${B2_APPLICATION_KEY_ID}
    key = ${B2_APPLICATION_KEY}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: rclone-sync
  namespace: backup
spec:
  schedule: "0 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
          - name: rclone
            image: rclone/rclone:latest
            args:
            - sync
            - /data
            - b2:${B2_BUCKET}
            - --verbose
            - --checksum
            volumeMounts:
            - name: data
              mountPath: /data
              readOnly: true
            - name: rclone-config
              mountPath: /config/rclone
              readOnly: true
          volumes:
          - name: data
            persistentVolumeClaim:
              claimName: gato-bucket-pvc
          - name: rclone-config
            configMap:
              name: rclone-config
